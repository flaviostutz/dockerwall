version: '3.5'

services:

  test1:
    image: busybox:1.29
    command: sh -c "ping www.uol.com.br"
    labels:
      - "dockerwall.outbound=www.yahoo.com"
    networks:
      - test

networks:
  test: